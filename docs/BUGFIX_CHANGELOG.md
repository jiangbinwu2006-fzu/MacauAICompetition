# 澳门文旅智联缺陷修复日志

更新时间：2026-07-27

## 本轮用户反馈

### BUG-01 偏好设置后生成路线无法重置

- 现象：路线生成后只有重新生成、撤回和恢复原始路线，没有清空当前路线的操作。
- 根因：后端已有 `DELETE /api/trips/current`，前端 API 和行程工具栏没有接入。
- 修复：新增清空路线按钮，清除后端会话行程、前端行程状态、地图线路、公交详情和弹窗状态。
- 状态：已完成；前端生产构建通过，后端重置接口已存在并由会话接口契约覆盖。

### BUG-02 公交路线缺少高德智能规划详情

- 现象：公交方案只有概要指标和地图路线，没有说明乘坐线路、上车站、下车站及站点位置。
- 根因：高德 `AMap.Transfer` 使用 `extensions: base`，且回调结果未解析。
- 修复：切换为完整公交结果，解析公交线路、上车站、下车站、途经站、耗时及站点坐标；上下车站以不同颜色图标标记在地图，并同步显示在行程面板。
- 状态：已完成；前端生产构建通过。实际线路、站名与坐标由运行时高德在线结果决定，高德无结果时继续使用静态降级路线。

### BUG-03 后端频繁自动关闭

- 现象：前端偶发显示点位读取失败，检查发现 `8290` 后端进程已退出。
- 根因：开发服务曾通过临时命令父进程启动，父进程结束时 Java 子进程被一并终止；后端日志没有 Spring Boot 崩溃异常。
- 修复：新增独立隐藏进程启动器和后端守护脚本；异常退出 3 秒后自动重启，PID 与输出统一记录到 `.runtime`；新增配套停止脚本。
- 状态：已完成并验证。测试中强制结束后端 PID `13944`，守护器自动启动新 PID `41612`，`8290` 恢复监听且目录接口返回 60 条点位。
- 2026-07-29 重启加固：处理进程环境同时包含 `Path/PATH` 时 PowerShell `Start-Process` 因重复键退出的问题；启动器和后端守护器现在会先规范化进程级路径变量，再拉起 Java 与 Node。

### BUG-04 运营测试系统区域与布局问题

- 现象：区域只能从固定值中选择，页面控制区偏窄；场馆关闭没有清晰的场馆选择流程。
- 修复：区域字段改为可选择也可自由输入；桌面端使用明确的左侧测试参数、右侧事件结果布局并扩大页面有效宽度；选择“场馆关闭”时自动筛选场馆类点位并显示“选择关闭场馆”。
- 状态：已完成；Vue 模板编译与生产构建通过，区域输入、条件点位筛选和左右布局均已进入构建产物。

## 涉及文件

- `ai-tourism-frontend/src/utils/api.js`
- `ai-tourism-frontend/src/utils/i18n.js`
- `ai-tourism-frontend/src/components/TripPanel.vue`
- `ai-tourism-frontend/src/views/Explore.vue`
- `ai-tourism-frontend/src/views/Ops.vue`
- `scripts/backend-supervisor.ps1`
- `scripts/start-project.ps1`
- `scripts/stop-project.ps1`
- `start-project.cmd`
- `stop-project.cmd`

### BUG-05 后端重启后偏好显示保存失败

- 现象：后端重启后点位可以读取，但当前页面保存或重置偏好失败。
- 二次定位：游客令牌自动续期已经成功，但浏览器还保存着一个旧的正式登录令牌；请求头始终优先选取该令牌，所以新的游客令牌没有真正用于后续请求。
- 修复：遇到 HTTP 401 或业务码 `1101` 时先尝试刷新正式令牌；刷新失败后，仅在偏好、路线和反馈等游客业务接口清理失效登录状态、创建匿名会话并重试一次。页面身份提示同时切换为临时游客。
- 验证：后端日志确认游客续期请求及重试 `PUT /api/preferences` 返回 200；前端生产构建通过。
- 状态：已完成。

### BUG-06 地图景点图标退化为黑白

- 用户复验：分类颜色恢复后，现有水滴式标记观感仍不理想，要求改为卡通样式。
- 修复：取消水滴造型，改为 24px 彩色卡通贴纸；使用浅色底、深色分类图形、彩色描边、小高光和轻微悬浮动效。
- 分类风格：景点珊瑚红、文化葡萄紫、自然薄荷绿、交通天蓝、公共服务青绿、餐饮杏橙、零售玫红、康养嫩绿。
- 状态：已完成，前端生产构建通过。

### BUG-07 模拟事件定位不够清晰

- 现象：运营事件只显示区域代码和 `P004` 一类点位代码，地图没有突出显示事件位置，游客提醒也无法直接定位。
- 修复：测试表单新增地点、区域、点位编码及高德坐标预览；选择地点后运营地图自动聚焦，并按封路、暴雨、封馆显示不同事件图标和影响范围圈。
- 事件列表：改为显示实际地点名称和中文区域名称，新增地图定位按钮，不再以点位代码作为主要位置表达。
- 游客提醒：新增“影响地点”文字和“在地图定位”按钮，可直接聚焦受影响点位并打开地点详情。
- 定位原则：事件标记位置由运行时 `AMap.PlaceSearch` 查询；高德无结果时明确显示未定位，不使用人工坐标伪装成功。
- 状态：已完成，前端生产构建通过。

## 验证结果

- 前端生产构建：通过，Vite 转换 56 个模块。
- 后端自动化测试：8 项通过，0 失败，0 错误。
- MySQL 集成：通过。
- 后端守护恢复：通过，模拟退出后自动重启并恢复 60 条点位读取。
- 前后端运行端口：`3001`、`8290`。
- 游客会话重启恢复：通过，旧令牌 `1101` 后新会话保存返回 `0`。

### BUG-08 偏好缺少游客所在地，无法匹配附近景点

- 现象：偏好仅包含兴趣、时间和交通等条件，路线引擎固定使用默认点位作为起点，无法按游客当前位置优先匹配附近景点。
- 前端修复：偏好面板新增“使用当前位置”，仅在游客主动点击后请求浏览器定位权限；同时提供澳门点位手动起点和清除位置功能。定位拒绝、超时、浏览器不支持及身处澳门范围外均有明确提示。
- 隐私约束：经纬度、位置名称和来源仅写入当前 Sa-Token 游客会话，不写入用户表或其他持久化存储，重置偏好或会话过期后清除。
- 后端修复：偏好接口新增当前位置经纬度、名称及 `GPS/MANUAL` 来源，校验坐标必须成对出现且位于澳门范围；路线引擎按当前位置寻找最近有效点位，并从真实坐标计算首段路程。
- 四语支持：简体中文、繁体中文、英文和葡文均新增定位操作及异常提示。
- 验证：后端 9 项测试全部通过（含所在地路线回归测试），前端 Vite 生产构建通过；运行态使用氹仔坐标生成路线，首站区域为 `TAIPA`，首段从“氹仔当前位置”出发约 378 米，路线可行且无硬约束冲突。
- 状态：已完成，等待用户验收。

### BUG-09 冲突事件只能选择大炮台，改线操作缺少策略组件

- 现象：运营端事件表单使用单个 `poi_code`，并默认固定为 `P004` 大炮台；游客端只有两个紧邻按钮，无法清楚了解局部改线和整程重排的差异。
- 多地点修复：移除大炮台默认值，受影响地点改为复选清单，支持按区域与事件类型筛选、选择当前区域全部、清空以及已选数量显示。
- 地图修复：通过高德地点查询同时解析所有已选地点，在运营地图绘制多个事件标记和影响范围；快速连续勾选时使用渲染版本控制，避免旧查询覆盖最新选择。
- 事件提交：`affected_poi_codes` 直接提交完整数组，普通发布和模拟封路均支持多个地点；未选择地点时明确拦截。
- 游客端组件：路线命中事件后展示受影响节点清单，以及并列的“局部改线”和“整程重排”策略组件。组件分别说明保留范围、重算范围、适用情形和执行状态。
- 结果展示：改线完成后显示策略类型、时间变化、步行变化和绕行变化；局部与全局操作均继续支持撤回及恢复原始路线。
- 四语适配：两种改线策略的标题、说明、约束提示和结果标签覆盖简中、繁中、英文、葡文。
- 验证：真实接口以 `T014,T013` 两地点制造冲突；局部改线返回 `LOCAL_REROUTE`、整程重排返回 `GLOBAL_REROUTE`，均为可行路线且残留冲突点为 0。后端 9 项测试通过，前端 Vite 构建通过。
- 状态：已完成，等待用户验收。

### BUG-10 事件模拟缺少具体地址选项

- 现象：多选清单虽然包含澳门目录地点，但只显示地点名称、区域和代码，没有街道地址，无法按具体地址判断事件影响位置。
- 根因：现有目录 DTO 不包含地址字段，先前仅在地点被勾选后通过高德查询坐标，没有提前获取候选地址。
- 修复：区域或事件类型变化后，使用 `AMap.PlaceSearch` 分批解析当前候选地点，缓存高德返回的地址与坐标，并直接显示在每个可选项中。
- 搜索：新增“搜索地点名称或高德地址”，可用地点名、地址或点位代码过滤当前区域选项。
- 状态处理：地址加载中、高德未返回地址和无匹配结果均有明确显示；批量请求限制为每组 4 个，避免同时请求过多。
- 数据原则：不新增人工地址或人工坐标，高德没有返回结果时明确标记失败，不伪造地址。
- 验证：目录接口返回 60 个候选点，澳门半岛 20、氹仔 14、路氹 12、路环 14；前端 Vite 56 模块生产构建通过，`3001` 与 `8290` 服务健康。
- 状态：已完成，等待用户验收。

### BUG-11 地点选择区不可见，发布事件反复提示必须选点

- 用户截图确认：运营页首屏被热力图占据，事件表单只露出顶部字段，地点多选清单位于视口下方；顶部“注入当前点位封路”在未选点时只提示错误，没有引导到清单。
- 布局修复：测试事件参数区调整到指标栏之后、热力图之前，进入运营页即可优先看到事件配置和地点清单。
- 引导修复：地点区增加“必选”标识；未选地点时点击顶部注入或发布事件，页面自动滚动至地点清单，并以红色边框高亮具体问题。
- 加载修复：不再静默吞掉目录接口异常。地点目录现在显示加载状态；请求失败或返回空目录时显示原因和“重试加载”按钮。
- 地址选项：候选项继续显示地点名称、高德地址、区域及点位代码，支持搜索、复选、区域全选与清空。
- 验证：目录接口当前返回 60 个地点；前端 Vite 56 模块构建通过，页面服务 `3001`、后端 `8290` 正常。
- 状态：已完成，等待用户验收。

### BUG-12 改线残留冲突、未按当前位置规划且路线时长固定

- 现象一：局部改线或整程重排完成后，结果中仍可能保留被事件影响的地点，却显示为改线成功。
- 现象二：改线继续使用创建行程时的旧偏好和起点，未读取用户刚更新的当前位置；候选点缺少稳定的距离排序，表现为随机改线。
- 现象三：路线点位数量固定，用户设置更长或更短的出发、结束时间后，仍按“半日路线”生成。
- 冲突修复：改线前读取当前会话的最新偏好；候选点统一排除所有生效事件影响点、已使用点和未受影响的保留点。改线求解结束后再次执行全部硬约束校验，只要仍有冲突就拒绝提交新版本并返回具体原因，不再伪装为成功。
- 当前位置修复：局部替换按“最新当前位置或前一未受影响节点 → 候选点 → 下一未受影响节点”的新增距离排序，并加入区域、兴趣和无障碍条件；整程重排从最新位置重新选择邻近区域点位。相同分数使用稳定点位代码排序，结果可复现。
- 时间窗修复：路线目标点位数改为由 `departure_time` 到 `latest_end_time` 的可用分钟数动态计算，范围为 1–8 个点，并确保包含全部必去点；当预计结束时间或步行上限冲突时，逐个移除非必去的末端点并重新求解，直至满足硬约束或明确报告无解。
- 前端文案：四语界面的“生成半日路线”改为“按偏好时间生成路线”，空状态同步说明路线依据偏好中的开始和结束时间生成。
- 自动化验证：后端 10 项测试全部通过，新增覆盖短/长时间窗点位数量变化、更新当前位置后的整程重排、冲突点彻底移除和首段起点更新；前端 Vite 56 模块生产构建通过；后端打包成功。
- 真实接口验证：`09:00–14:00` 生成 4 个点，`09:00–18:00` 生成 7 个点；模拟封锁 `P005` 后，局部改线与整程重排均返回 `feasible=true`、冲突数 0、残留 `P005` 为 false，首段均从最新的“氹仔当前位置”出发，整程重排首站区域为 `TAIPA`。
- 状态：已完成，等待用户验收。

### BUG-13 整程重排在必去点被事件封锁时失效

- 现场条件：运营端模拟事件一次封锁澳门半岛全部 20 个点位，用户必去点也位于封锁范围内。
- 根因：旧逻辑同时要求“移除事件封锁点”和“必去点绝对保留”，两个条件相互矛盾，因此后端拒绝提交重排结果，前端看起来像按钮失效。
- 修复：用户主动点击局部或整程改线时，安全事件命中的必去点仅在该次改线中临时挂起，从路线移除并返回明确警告；未受影响的必去点继续严格保留。
- 界面修复：安全挂起说明以黄色提示显示；改线业务错误移到路线状态和改线组件上方，不再隐藏在长行程底部。改线成功后若当前路线已不受事件影响，后续重复请求会明确提示无需再改线。
- 自动化验证：后端共 10 项测试通过，新增“必去点被封锁”回归用例；前端 56 模块生产构建通过。
- 真实接口验证：封锁澳门半岛 20 个点位，并把 `P005` 设为必去点；整程重排返回 `GLOBAL_REROUTE` 和 `feasible=true`，冲突数 0，封锁点残留数 0，路线转入氹仔并从最新位置出发，同时返回 1 条安全挂起警告。
- 状态：已完成，等待用户验收。

### BUG-14 “使用当前位置”按钮文字溢出及空位置误判

- 现象：定位区的手动起点列按内容自动撑宽，导致“使用当前位置”按钮被压窄并溢出，长英文和葡文更明显。
- 布局修复：定位区改为受控比例两列，按钮使用 100% 宽度、稳定最小高度、居中换行和长词断行；移动端保持单列全宽。
- 空值修复：只有经纬度同时非空且为有效数字时才标记为已定位，未获取位置时不再错误显示 `0.00000, 0.00000` 和清除按钮。
- 可视验证：桌面端葡文“Usar localizacao atual”的内容宽高与按钮容器一致；`390×844` 移动视口下按钮溢出为 false，定位容器溢出为 false。
- 状态：已完成，等待用户验收。

### BUG-15 路线编号与高德地图正确地点错位

- 现象：路线规划完成后，地图上的 `1、2、3、4` 编号使用后端目录中的静态经纬度，而普通地点图标使用高德在线检索位置，导致同一地点出现坐标错位。
- 根因：路线渲染函数直接读取 `trip.stops` 的静态 `longitude/latitude`，没有复用地点目录已经通过 `AMap.PlaceSearch/Geocoder` 解析并缓存的真实位置。
- 修复：绘制路线前按 `poi_code` 匹配完整地点目录，并逐点等待高德位置解析；路线编号、编号点击详情及高德步行/公交路线端点统一使用 `amapPositions` 中的坐标。
- 降级原则：高德未返回某一地点的位置时，不再使用静态坐标绘制一个可能错误的编号；跳过该节点的编号和相邻绘线，并在控制台记录点位代码。
- 验证：前端 Vite 生产构建通过，共转换 56 个模块。重启后使用默认 `09:00-14:00` 偏好生成 4 点路线，`1 议事亭前地`、`2 玫瑰堂`、`3 澳门博物馆`、`4 圣老楞佐教堂` 的路线编号容器与同名高德地点标记逐项比较 `left/top`，4 项全部完全相等；未出现高德路线点位缺失警告。
- 运行状态：前端 `3001`、后端 `8290` 均已监听；目录接口返回 60 个点位，MySQL 连接正常。
- 状态：已完成，等待用户验收。

### BUG-16 行程地图改为高德原生路线与公交样式

- 需求：路线规划地图直接使用高德地图的呈现风格，不再使用 `1、2、3、4` 数字标记，并接入高德公交路线规划。
- 原因：原实现虽然调用了 `AMap.Walking` 和 `AMap.Transfer`，但设置了 `hideMarkers: true`，随后又叠加自定义数字节点、公交站点图标和目录卡通点位，遮盖了高德原生路线视觉。
- 修改：行程生成后隐藏普通目录点位和全部自定义数字节点；步行段使用 `AMap.Walking` 原生线路与起终点，公交段使用 `AMap.Transfer` 原生线路、上下车站和换乘标记。
- 公交策略：请求完整公交结果 `extensions: all`，优先使用高德 `LEAST_TIME` 策略；线路名称、上车站、下车站、途经站和耗时继续同步到左侧行程详情。
- 恢复：清空当前行程时自动清除高德路线服务并恢复普通澳门点位目录。
- 短途策略：相邻地点直线距离不足 600 米时使用高德原生步行规划，避免对极短路段强行请求公交；其余 `PUBLIC_TRANSIT` 路段继续调用高德公交换乘。
- 调用量优化：普通目录点位和行程节点直接复用后端目录已有经纬度，移除游客页对 60 个点位逐一执行 `PlaceSearch/Geocoder` 的逻辑；同一行程版本增加渲染键去重，切换面板、重复回显和目录刷新不会再次请求步行或公交规划。高德在线路线调用仅发生在新生成、改线或恢复了不同版本的行程时。
- 冷启动恢复：地图实例完成初始化时若会话中已有行程，会自动补画该行程；若没有行程才恢复普通目录点位，避免左侧已有路线但地图空白。
- 验证：前端 Vite 生产构建通过，共转换 56 个模块。运行态使用纯公交偏好恢复 4 点路线，自定义数字标记 `0`、目录卡通标记 `0`；高德原生步行起终点各 3 个、公交站 2 个，公交详情成功返回 `28B 路`、中华广场上车、风顺堂街下车及途经站，最终复测新增控制台错误为 `0`。
- 跨区复测：以 `09:00-18:00`、公交优先、必去“大三巴牌坊”和“澳门威尼斯人”生成 7 点路线，硬约束全部通过。高德返回“澳门博物馆 → 圣老楞佐教堂”和“郑家大屋 → 澳门威尼斯人”两段公交详情，包含 `1路/MT4路/26路`、氹仔线、上下车站与途经站；地图内自定义数字标记 `0`、自定义站点标记 `0`、目录点位标记 `0`，高德原生标记 `14`，本轮新增控制台错误 `0`。
- 运行状态：前端 `3001`、后端 `8290` 正常监听，目录接口返回 60 个点位。
- 状态：已完成，等待用户验收。

### BUG-17 固定保存两条一日往返演示路线

- 需求：为后续现场演示保留两条可重复载入的固定路线，一条从关闸口岸出发并返回关闸，一条从酒店出发并返回同一酒店；同时提供60个目录点位名称用于地址核对。
- 后端：新增 `POST /api/trips/demo/GATE_LOOP` 与 `POST /api/trips/demo/HOTEL_LOOP`。预设采用固定起点、固定顺序、`09:00-19:30` 时间窗、公交优先和6000米步行上限，载入后写入当前游客会话并可通过 `/api/trips/current` 恢复。
- 关闸环线：关闸广场出发，经卢廉若公园、塔石广场、东望洋炮台及灯塔、澳门格兰披治大赛车博物馆、澳门渔人码头、外港客运码头，最终返回关闸广场。
- 酒店环线：澳门威尼斯人出发，经官也街、大利来记猪扒包、龙环葡韵、永利皇宫表演湖、美狮美高梅艺术空间、澳门巴黎人铁塔，最终返回澳门威尼斯人。
- 前端：行程页新增“固定演示路线”双按钮，支持简中、繁中、英文和葡文；已有路线时仍可一键切换另一个预设。
- 公交演示保护：高德公交查询使用澳门行政区代码 `820000`；若结果包含珠海、拱北、横琴等跨境站名或单段超过75分钟，则清除错误公交结果并展示静态降级线路，防止现场出现明显跨境绕行。
- 点位核对：新增 `MACAU_POI_ADDRESS_REVIEW.md`，按澳门半岛20、氹仔14、路氹12、路环14整理全部60个点位的代码、名称和当前经纬度。
- 自动化验证：后端完整测试12项通过、0失败、0错误；最终演示闭环回归测试6项通过；前端 Vite 生产构建通过，共转换56个模块；核对表点位行数为60。
- 运行态验证：关闸路线7点、16:29返回关闸，酒店路线7点、17:02返回澳门威尼斯人，两条路线均 `feasible=true`、地图数字标记0；酒店路线成功显示澳门50路及氹仔线的上下车站，本轮新增控制台错误0。
- 状态：已完成，等待用户验收。

### BUG-18 多段路线重复显示多个“起点”标记

- 现象：路线由多个步行或公交路段组成，每次调用高德 `Walking/Transfer` 都自动绘制一组“起/终”标记，导致数个景点同时显示为起点。
- 修复：关闭各分段高德服务的自动端点标记，改为在整条路线完成后统一绘制端点；普通路线只显示一个“起”和一个“终”，起终点相同的往返路线只显示一个双色“起/终”。
- 公交站点：解析高德公交结果后继续绘制小型公交站图标，上车站和下车站使用不同颜色，不再使用“起”字，站名仍可通过标记标题和左侧公交详情查看。
- 往返首段：固定演示路线补画关闸或酒店到第一个景点的首段，地图线路现在覆盖完整出发、游览、返程闭环。
- 验证：前端 Vite 生产构建通过，共转换56个模块。酒店往返路线端点标记1个且文本为“起/终”，公交站点8个；普通路线端点标记2个且分别为“起”“终”；两种场景地图数字标记均为0，新增控制台错误0。
- 状态：已完成，等待用户验收。

### BUG-19 普通路线统一使用“智能公交路线”展示

- 需求：除“关闸一日往返”和“酒店一日往返”两条固定演示路线外，普通偏好生成路线及后续改线结果也使用同一套高德公交规划展示；功能名称统一去除供应商品牌，改为“智能公交路线”。
- 实现：固定演示路线与普通路线继续共用 `Explore.vue` 的 `AMap.Transfer` 渲染链路，不按路线来源设置分支；所有 `PUBLIC_TRANSIT` 路段均可展示线路、上车站、途经站、下车站与耗时。相邻点不足 600 米时仍优先使用步行规划，以减少不必要的高德接口调用。
- 四语命名：简中“智能公交路线”、繁中“智能公交路線”、英文“Smart transit route”、葡文“Rota inteligente de autocarro”。
- 验证：前端 Vite 生产构建通过，共转换 56 个模块；静态检索确认旧名称与旧翻译残留为 0。运行态使用非预设普通路线“议事亭前地 → 玫瑰堂 → 澳门博物馆 → 圣老楞佐教堂”复测，页面显示“智能公交路线”，返回 1 个公交方案，并完整显示海边新街上车、途经站、河边新街/街市下车；本轮新增控制台错误 0。
- 状态：已完成，等待用户验收。

### BUG-20 地图目录点位及部分景点坐标偏移

- 现象：游客地图把数据库中的 60 个目录示例经纬度直接绘制为自定义标记，部分文化、景点图标落在水面或与高德原生地点不重合；路线节点也会继续复用这些示例坐标。
- 根因：为减少高德调用而移除了在线地点解析，但示例目录坐标不具备地图展示精度；同一地点在目录和高德中的名称还可能存在“关闸广场/关闸总站”“东望洋炮台及灯塔/东望洋灯塔”等差异。
- 修复：全域浏览不再绘制 60 个静态自定义目录标记，直接保留高德底图原生地点；用户选择单个地点或生成路线时，才通过 `AMap.PlaceSearch` 按名称解析真实坐标。解析结果按 `poi_code` 在当前页面会话缓存，并对并发请求去重。
- 名称兼容：候选点必须位于澳门范围内，并按名称字符相似度选择；首次未命中时仅对 5 个已确认存在名称差异的点位使用一次短名称重试。关闸演示路线使用高德“关闸总站”作为实际起终点。
- 安全降级：高德未返回可信澳门坐标时不再使用可能错误的示例坐标绘图，避免把景点伪装到水面或其他区域。
- 验证：前端 Vite 生产构建通过，共转换 56 个模块。全域地图自定义目录标记为 0；选择“大三巴牌坊”后仅生成 1 个标记并与高德原生地点重合；关闸一日往返路线显示 1 个“起/终”合并端点、4 个公交方案和 8 个上下车站标记，本轮坐标警告与控制台错误均为 0。
- 状态：已完成，等待用户验收。

### BUG-21 当前定位位置缺少地图标记

- 现象：游客通过浏览器 GPS 或手动选择澳门起点后，偏好面板会显示位置名称和坐标，但地图上没有明确的“我的当前位置”标记。
- 修复：偏好面板新增会话级 `location-change` 事件；定位成功、手动选择、加载已有会话位置和清除位置时均同步通知地图。地图新增独立的蓝色脉冲定位标记，并提供“我的当前位置：地点名称”标题。
- 坐标准确性：GPS 位置直接使用浏览器返回坐标；手动选择目录地点时按 BUG-20 的高德地点解析结果显示，不再使用可能偏移的示例坐标。
- 生命周期：当前位置标记独立于景点、路线和公交站覆盖物，生成路线或切换页签时不会被移除；清除位置和重置偏好时立即移除。偏好面板改为常驻显示切换，避免未保存的会话位置在切换“偏好/行程/点位”时丢失。
- 验证：前端 Vite 生产构建通过，共转换 56 个模块。手动设置“大三巴牌坊”后地图标记数为 1，切换行程再返回偏好仍为 1，清除后为 0，再次设置后恢复为 1；运行态新增控制台错误 0。
- 状态：已完成，等待用户验收。

### BUG-22 全域地图未正常显示全部分类图标

- 现象：修正错误坐标后，全域浏览只保留高德底图原生点位，项目目录中的景点、文化、餐饮、自然、公共服务、社区零售和交通分类图标没有完整显示。
- 根因：为避免继续绘制错误的示例坐标，BUG-20 移除了 60 个目录标记，但没有在高德真实地点解析完成后恢复项目分类图标。
- 修复：恢复全量目录标记，并在绘制前逐项通过 `AMap.PlaceSearch` 获取澳门范围内的真实位置；增加名称相似度筛选、常见名称别名和通用重试，避免同名或简称导致漏标。解析采用每批 2 个请求的渐进式渲染，并将成功结果缓存 30 天，后续打开页面优先复用缓存，减少高德调用。
- 状态一致性：分类、语言或目录刷新时使用渲染版本号取消旧批次，防止旧请求覆盖最新筛选结果；行程生成后隐藏目录图标，清空行程后自动恢复。
- 验证：运行态简中全分类目录共 60 个点位，高德真实坐标解析成功并显示 `60/60` 个分类图标；控制台新增坐标警告和错误均为 0。
- 状态：已完成，等待用户验收。

### BUG-23 使用当前位置生成路线仍从最近景点开始显示

- 现象：使用 GPS 或手动当前位置生成路线时，后端虽然计算了“当前位置到首个景点”的第一段，但地图从最近景点开始绘制，视觉上像是以最近景点代替了用户位置。
- 根因：普通路线渲染只遍历景点之间的相邻路段，漏掉了后端已经返回的当前位置首段；地图起点也继续取第一个景点坐标。
- 修复：普通路线将当前定位标记的真实高德/GPS 坐标作为唯一地图起点，先绘制“当前位置 → 第一站”，再绘制后续景点路段；固定演示环线仍按预设起终点绘制。当前位置标记直接承担起点提示，只额外绘制唯一终点，避免重复出现起点图标。
- 验证：以“澳门威尼斯人”为当前位置保存偏好并生成普通路线，页面显示硬约束全部通过；地图保留 1 个“我的当前位置”标记、1 个“行程终点”标记、0 个目录干扰标记，并从当前位置绘制首段后继续连接 4 个行程点位。控制台新增错误为 0。
- 状态：已完成，等待用户验收。
